= content_for :panel do 
  = render partial: 'shared/panel', locals: {titulo: "Participante", accion: "Editar", |
    botones: [{ruta: participantes_path, texto: 'Listado'}, {ruta: participante_path(@participante), texto: 'Ver'} ] }

= render :partial => "shared/errores_crud", :locals => { :registro => @participante }

= render 'form'
