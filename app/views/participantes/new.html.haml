= content_for :panel do 
  = render partial: 'shared/panel', locals: {titulo: "Participante", accion: "Nuevo", |
    botones: [{ruta: participantes_path, texto: 'Listado'} ] }

= render :partial => "shared/errores_crud", :locals => { :registro => @participante }

= render 'form'
