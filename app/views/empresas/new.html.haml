= content_for :panel do 
  = render partial: 'shared/panel', locals: {titulo: "Empresa", accion: "Nueva", |
    botones: [{ruta: empresas_path, texto: 'Listado'} ] }

= render :partial => "shared/errores_crud", :locals => { :registro => @empresa }

= render 'form'


